import React, { FC } from 'react';
import PromoSection from '../components/PromoSection';

const Main: FC = () => {
    return (
        <main>
           <PromoSection/>
        </main>
    );
};

export default Main;